#include "StringCalculator.h"

int StringCalculator::add(const std::string& input) {
    if (input.empty()) {
        return 0;
    } else {
        // Logic for adding non-empty string (not specified in your original request)
        return -1; // Placeholder for non-empty input case
    }
}

